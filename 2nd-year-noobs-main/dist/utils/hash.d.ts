/**
 * Computes a SHA-256 hash for the given data.
 * @param data Buffer, string, or Uint8Array to hash.
 * @returns Hex-encoded SHA-256 hash string.
 */
export declare function computeHash(data: string | Uint8Array): string;
